https://nextjs.org/docs/app/getting-started/css?utm_source=chatgpt.com#tailwind-css

instalar Tailwind css (framework)

    pnpm add -D tailwindcss @tailwindcss/postcss

Cuando cambio a Tailwind el vite.config.ts que era:

    import { defineConfig } from 'vite'
    import react from '@vitejs/plugin-react'

    // https://vite.dev/config/
    export default defineConfig({
    plugins: [react()],
    })

cambia a 

    import { defineConfig } from 'vite';
    import tailwindcss from '@tailwindcss/postcss';

    export default defineConfig({
    plugins: [tailwindcss()]
    });
